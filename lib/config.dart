// Base URL for the API
final String api = 'http://localhost:3000'; // Update the URL to match your Node.js backend

// Endpoint URLs
final String registration = '$api/users/register';
final String login = '$api/users/login'; // Define the login endpoint

// Other endpoint URLs as needed

// MongoDB collection-specific endpoints (optional, if your API exposes endpoints for MongoDB collections)
final String designs = '$api/designs';
final String orders = '$api/orders';
final String tailors = '$api/tailors';
final String users = '$api/users';
